# Round 3 — space membership/moderation mutation review

Frozen finding before correction:

1. `SN_Spaces_Part_5::change_ban()` emitted a reliable outbox fact for `space.member_banned`, but the inverse successful `unban` transition committed without an outbox event. That leaves downstream projections/reconciliation unable to react reliably to revocation of a ban and breaks the event/fact symmetry expected for canonical state transitions.

Status: DEFECT. Ledger frozen before correction.
