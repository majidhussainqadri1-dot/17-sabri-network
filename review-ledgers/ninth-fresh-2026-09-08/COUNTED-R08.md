# Counted Round 8 — privacy export/erasure and encrypted-message compatibility

Reviewed WordPress personal-data export/erasure integration, batched message/attachment erasure, report legal-hold handling, direct-conversation/call teardown, contact/follow/block cleanup, audit minimization and the high-priority compatibility exporter that replaces raw encrypted `SNE1` message bodies with authorized in-memory decrypted user-readable content (or an explicit unavailable marker on decryption failure).

No new proved defect found.

Status: CLEAN. Ledger frozen before Round 9.
